# Proxy frontal PulsaNet (HTTP). Para HTTPS: reemplaza :80 por tu dominio
# y deja que Caddy gestione certificados automáticamente.
:80 {
	encode gzip

	handle /api* {
		reverse_proxy api:4000
	}

	handle /socket.io* {
		reverse_proxy api:4000
	}

	handle {
		reverse_proxy web:80
	}
}
