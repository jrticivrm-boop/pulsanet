# LiveKit — desarrollo (LAN Wi-Fi + 4G via IP publica / UPnP)
# node_ip / turn.domain se sincronizan con infra\Sync-PublicIp.ps1
# Arranque: livekit-server --config infra/livekit.dev.yaml --dev --udp-port 7882 --node-ip <IP_PUBLICA>

port: 7880
bind_addresses:
  - "0.0.0.0"

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
  node_ip: 189.175.60.174
  advertise_internal_ip: true
  congestion_control:
    enabled: true
    allow_pause: false

turn:
  enabled: true
  domain: 189.175.60.174
  udp_port: 3478

logging:
  level: info
