# Movido

El material auxiliar está en **C:\pulsanet_soporte\** (no forma parte del producto portable).

- Documentos, APK, Brand, Logs de archivo, Scripts one-shot, Cursor/IA
- UI del producto: `frontend\` (prod `:5173` en `C:\pulsanet`, DEV `:5273` en `C:\pulsanet-dev`)
- Secretos de runtime: `..\infra\secrets\` (van con la carpeta; no versionar)
- Logs en caliente si no existe pulsanet_soporte: `..\var\logs\`

Esta carpeta `Soporte\` dentro del repo queda como puntero. El programa no la necesita para arrancar.
